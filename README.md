# AI-Resume-Reviewer
Resume Reviewer using LangGraph + Gemini with a real feedback/iteration loop. Here's the plan:  6 specialized agents wired via LangGraph StateGraph Gemini LLM for intelligent extraction and suggestions Evaluation loop that re-runs improvement until score threshold is met Streamlit UI + Jupyter notebook
